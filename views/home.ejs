<!DOCTYPE html>
<html lang="en">
  <head>
    <style>
      /* Your existing styles */
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: #1e1e1e;
        color: #f8f8f8;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        min-height: 100vh;
        overflow-x: hidden;
        width: 100%;
        box-sizing: border-box;
      }

      .back-button {
        position: absolute;
        top: 20px;
        left: 20px;
        background-color: transparent;
        border: none;
        color: #d4af37;
        font-size: 1.5rem;
        cursor: pointer;
        text-decoration: none;
        font-weight: bold;
        text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.4);
        transition: color 0.3s ease;
      }

      .back-button:hover {
        color: #ffc400;
      }

      h1 {
        color: #d4af37;
        text-align: center;
        margin-top: 60px;
        width: 100%;
        font-size: 36px;
        text-transform: uppercase;
      }

      .container {
        background: #333;
        padding: 40px;
        border-radius: 15px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
        width: 100%;
        max-width: 450px;
        text-align: center;
        word-wrap: break-word;
        margin-top: 40px;
      }

      form {
        display: flex;
        flex-direction: column;
        gap: 20px;
        width: 100%;
        background: #333;
        padding: 20px;
        border-radius: 15px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
        margin-top: 20px;
        box-sizing: border-box;
      }

      label {
        font-weight: bold;
        color: #f8f8f8;
        text-align: left;
        font-size: 18px;
      }

      input {
        padding: 12px;
        border: 1px solid #555;
        border-radius: 8px;
        font-size: 16px;
        width: 100%;
        display: block;
        background-color: #444;
        color: #f8f8f8;
        box-sizing: border-box;
      }

      input:focus {
        outline: none;
        border: 1px solid #d4af37;
        background-color: #555;
      }

      button {
        background-color: #d4af37;
        color: #1e1e1e;
        padding: 12px;
        border: none;
        border-radius: 8px;
        font-size: 18px;
        cursor: pointer;
        transition: all 0.3s;
        width: 100%;
        box-sizing: border-box;
      }

      button:hover {
        background-color: #f8f8f8;
        color: #d4af37;
        transform: scale(1.05);
      }

      /* Table Styles */
      .table-container {
        width: 100%;
        max-width: 100%;
        margin-top: 40px;
        padding: 20px;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        background-color: #333;
        border-radius: 12px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        table-layout: fixed;
        overflow: hidden;
      }

      th,
      td {
        padding: 12px;
        text-align: center;
        border-bottom: 1px solid #555;
        font-size: 1rem;
        color: #f8f8f8;
        word-wrap: break-word;
        overflow-wrap: break-word;
      }

      th {
        background-color: #d4af37;
        color: white;
        font-size: 1.1rem;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      td {
        color: #f8f8f8;
      }

      tr:hover {
        background-color: #444;
        transform: scale(1.03);
        transition: transform 0.3s ease, background-color 0.2s ease;
      }

      .delete-button {
        background-color: #e74c3c;
        color: white;
        border: none;
        padding: 8px 16px;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.3s ease;
        font-size: 14px;
      }

      .delete-button:hover {
        background-color: #c0392b;
        transform: scale(1.05);
      }

      .redirect-button {
        background-color: #2E8B57;
        color: white;
        border: none;
        margin-top: 10px;
        padding: 8px 16px;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.3s ease;
        font-size: 14px;
      }

      .redirect-button:hover {
        background-color: #3dcc7b;
        transform: scale(1.05);
      }

      @media (max-width: 768px) {
        h1 {
          font-size: 28px;
        }

        .container {
          padding: 30px;
          max-width: 90%;
        }

        input {
          font-size: 14px;
        }

        button {
          font-size: 16px;
        }

        .table-container {
          padding: 10px;
        }

        table th,
        table td {
          font-size: 0.9rem;
        }

        .delete-button,
        .redirect-button {
          padding: 6px 12px;
        }
      }

      @media (max-width: 480px) {
        h1 {
          font-size: 24px;
        }

        .container {
          padding: 25px;
          max-width: 95%;
        }

        label {
          font-size: 16px;
        }

        input {
          font-size: 14px;
        }

        button {
          font-size: 14px;
        }

        .table-container {
          padding: 5px;
        }

        table th,
        table td {
          font-size: 0.85rem;
        }

        .delete-button,
        .redirect-button {
          padding: 5px 10px;
          font-size: 12px;
        }

        table {
          font-size: 0.9rem;
        }

        table td,
        table th {
          padding: 10px;
        }
      }
    </style>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortener</title>
  </head>
  <body>
    <a href="/land" class="back-button">← Back</a>
    <h1>URL Shortener</h1>
    <% if (locals.id) { %>
    <p style="color: white; font-size: 1.2rem; text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.4);">URL Generated: <a href=" http://localhost:8000/url/<%= id %>">https://url-shortner-project-1-zv05.onrender.com/url/<%= id %></a></p>
    <% } %>

    <div class="container">
      <form method="POST" action="/url">
        <label>Enter Your Original URL</label>
        <input type="text" name="url" placeholder="https://example.com" />
        <button type="submit">Generate</button>
      </form>
    </div>

    <div class="table-container">
      <% if (locals.urls) { %>
      <table>
        <thead>
          <tr>
            <th>S. No</th>
            <th>ShortID</th>
            <th>Redirect</th>
            <th>Clicks</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          <% urls.forEach((url, index) => { %>
          <tr>
            <td><%= index + 1 %></td>
            <td><%= url.shortId %></td>
            <td><%= url.redirectURL %></td>
            <td><%= url.visitHistory.length %></td>
            <td>
              <button class="delete-button" onclick="deleteUrl('<%= url.shortId %>')">Delete</button>
              <button class="redirect-button" onclick="window.location.href='/url/<%= url.shortId %>'">Redirect</button>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <% } %>
    </div>

    <script>
      function deleteUrl(shortId) {
        if (confirm('Are you sure you want to delete this URL?')) {
          window.location.href = '/url/delete/' + shortId;
        }
      }
    </script>
  </body>
</html>
